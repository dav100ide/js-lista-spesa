'use strict';

const shoppingList = [
   'birra', // lista della spesa
   'vodka',
   'vino',
   'tequila',
   'rum',
   'acqua',
];

let i = 0;
while (i < shoppingList.length) {
   console.log(i);
   console.log(shoppingList[i]);
   i++;
}
